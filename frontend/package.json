{
  "name": "sentiment-ui",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --outfile=public/app.js --minify"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}
